# 상세 요구사항: 스마트 소상공인 지원금 매칭 및 규제 가이드 에이전트

이 문서는 서비스의 핵심 요구사항을 명확히 하기 위해 작성되었습니다. 요청하신 항목별로 정리합니다.

## 1. 문제 정의

- 정보 격차 문제: 소상공인은 공공 지원금, 보조금, 지자체 공모 등 유용한 재원 존재를 알더라도 최신 공고·조건·마감일을 모두 추적하기 어렵습니다.
- 시간·인력 제약: 소상공인은 운영에 전념하느라 정책 탐색과 신청 준비에 쓸 시간이 거의 없습니다. 결과적으로 기회를 자주 놓칩니다.
- 문서·법률 복잡성: 노동법, 계약서 조항, 세무·노무 규제는 복잡하고 자주 변경되어 사업자가 스스로 정확히 해석하기 어렵습니다.
- 분산된 정보와 신뢰성: 정책 정보가 여러 행정기관과 지자체에 분산되어 있고, 어떤 정보가 최신인지 판별하기 어렵습니다.

문제 요약: "시간과 전문성이 부족한 소상공인들이, 분산적이고 빠르게 변하는 공공지원·규제 정보를 제때 받지 못해 재정·법적 불이익을 받는 정보 격차를 해소"하는 것이 본 서비스의 목표입니다.

---

## 2. 사용자 시나리오 (User Flows)

각 시나리오는 현실적 행위자(페르소나)를 기준으로 작성합니다.

### 시나리오 A — 빠른 지원금 추천 및 신청
1. 사장님(음식점 운영자)은 웹/앱에 접속해 간단한 매장 기본정보(업종, 주소, 종사자 수, 사업자 등록상태)를 입력한다.
2. 에이전트가 등록 정보를 기반으로 현재 유효한 지원금 후보 목록을 실시간으로 추천한다(우선순위: 신청 가능성, 매칭 점수, 마감 임박 여부).
3. 사장님은 한두 건을 선택해 ‘관심 저장’하고, 지원금별로 필요한 서류목록과 마감일을 확인한다.
4. 준비 상태(서류 보유 여부)를 체크하면 시스템이 신청 준비도(%)를 계산해 우선순위를 제안한다.
5. 원할 경우 신청 링크로 이동하거나, 서류 준비 체크리스트를 출력/다운로드한다.

### 시나리오 B — 규제·계약서 체크
1. 사장님은 최근 변경된 노동법이 궁금해 에이전트에 질문한다(또는 알림으로 수신).
2. 에이전트는 핵심 변경사항을 요약해 제공하고, 사업자에게 영향이 큰 항목(최저임금·근로시간·주휴수당 등)을 강조한다.
3. 계약서의 특정 조항을 업로드하면(또는 텍스트 입력), 에이전트가 독소조항 여부를 자동으로 감지하고 위험도와 대응 권고를 제공한다.

### 시나리오 C — 지속적 알림과 지원
1. 사용자는 관심 지원금을 저장하고, 알림 설정(이메일/앱푸시)을 켠다.
2. 정책 변경/공고 업데이트/마감 임박 시 자동 알림을 수신한다.
3. 알림에서 바로 신청 페이지로 이동하거나, 에이전트에게 추가 질의를 해 실무 대응 가이드를 받는다.

---

## 3. 핵심 기능 (우선순위: Must → Should → Could)

아래는 제품의 최소 동작을 보장하는 핵심 기능(우선 Must)을 상세하게 기술합니다. 각 기능은 가능한 한 수용 기준(acceptance criteria)을 포함합니다.

### MUST (기술적으로 반드시 구현되어야 하는 핵심 기능)

1) 사용자 온보딩 및 매장 프로필
- 설명: 이메일/소셜 계정으로 가입, 매장 기본정보(상호, 주소, 업종 코드, 사업자번호, 연락처, 종업원 수) 등록.
- 수용 기준:
  - 사용자는 가입 후 매장 프로필을 생성할 수 있어야 함.
  - 프로필 정보는 수정/삭제 가능해야 함.

2) 지원금 매칭 엔진 (정책 데이터 파이프라인 포함)
- 설명: 공공·지자체·민간 지원금 데이터를 정기적으로 수집(parsing), 정규화(schema화)하고 사용자의 매장 정보와 규칙/모델 기반 매칭을 수행.
- 세부 동작:
  - 정책 데이터 스크래핑/수집 모듈(수동 업로드 대체 가능)
  - 데이터 정규화(업종 코드, 지역, 자격조건, 서류, 마감일 필드)
  - 매칭 알고리즘: 룰 기반 필터 + 가중치 점수 산출(예: 적합도, 우선순위, 예상지원금 규모)
- 수용 기준:
  - 사용자는 매장 조건에 맞는 정책 목록을 볼 수 있어야 함.
  - 각 정책 항목에 매칭 점수·필수서류·마감일 정보가 표시되어야 함.

3) 신청 준비도 계산 및 서류 체크리스트
- 설명: 사용자가 보유한 서류(사업자등록증, 통장사본 등)를 체크하면 지원금별 준비도를 계산해 우선순위를 제안.
- 수용 기준:
  - 사용자는 각 정책 옆에 준비도(%)를 확인할 수 있어야 함.
  - 준비도는 사용자가 체크한 서류 항목을 반영해 실시간 업데이트됨.

4) 실시간 알림(마감 임박, 정책 변경)
- 설명: 사용자가 저장한 관심 지원금 또는 필터에 따라 이메일/푸시/앱 알림 전송.
- 수용 기준:
  - 사용자는 알림 수단을 설정할 수 있어야 함(예: 이메일 켜기/끄기).
  - 마감 7일/3일/1일 전 알림(기본값)을 받을 수 있어야 함.

5) 법률·규제 가이드 챗(기본 LLM 연동)
- 설명: 사용자의 질의(자연어)에 대해 노동법·계약서·실무 대응안을 요약 제공. 초기에는 LLM + 규칙 기반 템플릿으로 동작.
- 수용 기준:
  - 사용자가 질의 시 핵심 요약(3~5문장)과 행동 권장안을 받음.
  - 민감하거나 불확실한 경우 '전문가 상담 권장' 알림 포함.

6) 계약서 독소조항 자동 감지(기본)
- 설명: 계약서 텍스트(붙여넣기 또는 파일 업로드)를 분석해 위험 문구를 하이라이트하고 위험요약을 제공.
- 수용 기준:
  - 업로드 후 요약과 상위 3개 위험조항을 보여줘야 함.
  - 사용자는 감지결과를 저장하거나 상담 요청을 할 수 있어야 함.

7) 대시보드(요약화면)
- 설명: 추천 정책, 관심 지원금, 최근 알림, 준비도 요약을 한 화면에서 파악.
- 수용 기준:
  - 로그인 후 대시보드 진입 시 위 항목들이 시각적으로 보여야 함.

8) 보안·개인정보 보호
- 설명: 사업자 번호 등 민감정보 암호화 저장, 최소 권한 원칙, 전송 시 TLS 적용.
- 수용 기준:
  - 개인정보는 암호화되어 저장됨(예: at-rest 암호화 표기).
  - 서비스는 HTTPS에서 제공됨(운영 환경).


### SHOULD (MVP 이후 조속히 추가할 우선 기능)

1) 관심 지원금 저장·공유 및 상태 추적(신청 상태 관리)
2) 관리자 대시보드(정책 데이터 관리, 통계)
3) 계약서 업로드 시 OCR + 구조화(이미지→텍스트)
4) 사용자별 맞춤 통계(추천 성공률, 사용자 행동 분석)


### COULD (추후 고려 기능)

1) 지자체 또는 공공 API 직접 연동(자동화 수준 향상)
2) 전문가(노무/법률)와의 연결(유료 상담 매칭)
3) 음성 인터페이스, 모바일 앱 푸시 고도화
4) 다국어 지원(외국인 사업자 대상)

---

## 4. 핵심 기능 상세(실무 구현 관점)

아래는 핵심 기능 중 특히 반드시 먼저 안정적으로 구현해야 할 부분들의 구현 고려사항입니다.

### 정책 데이터 파이프라인
- 스크래퍼(크론 기반)로 공공 포털/지자체 RSS 및 공고 페이지 수집
- 수집 후 정규화: 업종(표준코드 매핑), 지역 코드, 자격조건(텍스트→구조화), 필요서류 목록 추출
- 메타데이터 버전 관리: 데이터 변경 이력 저장(변경 전/후) 및 알림 트리거

### 매칭 엔진
- 초기: 룰 기반 필터(정규표현식·키워드 기반)로 빠르게 매칭
- 중기: 사용 피드백·클릭률을 활용한 가중치 조정(간단한 랭킹 모델)
- 결과 노출: 매칭 점수, 이유(어떤 조건이 일치했는지) 노출

### 법률·규제 챗
- LLM 프롬프트 템플릿을 미리 설계해 정확도 향상
- 내부 규칙 엔진으로 ‘불확실’ 케이스를 잡아내고 전문가 안내 문구 삽입
- 답변 로그 저장(검토용) 및 피드백 루프

### 계약서 분석
- 텍스트 입력 우선 지원(빠른 MVP)
- 추후 OCR·파일 업로드 지원
- 위험도 감지 룰: 해지조건, 위약금, 자동갱신, 손해배상 범위 등 키워드 탐지

---

## 5. 수용 기준 요약 (Acceptance Criteria)

- 사용자는 회원가입 후 매장 프로필을 등록할 수 있다.
- 시스템은 매장 조건에 맞는 지원금 목록을 반환하고, 각 항목에 매칭 점수·서류·마감일을 표시한다.
- 사용자는 관심 지원금을 저장하고, 관련 알림을 수신할 수 있다.
- 사용자는 챗 인터페이스로 법률·규제 관련 간단한 질문에 대해 요약 답변을 받을 수 있다.
- 계약서 텍스트 업로드 시 상위 위험조항 3개를 자동으로 검출한다.

---

## 6. 다음 단계 제안

1. 정책 데이터 샘플(공고 50건) 수집 및 정규화 시범
2. 간단한 룰 기반 매칭 엔진 프로토타입 구현
3. 챗 프롬프트 템플릿 설계 및 샘플 Q&A 수집
4. 사용자 인터뷰(5~10명)로 우선순위 검증

추가로 원하시면 이 문서를 기반으로 API 스펙(REST), DB 스키마, 또는 화면 와이어프레임(HTML 샘플)을 바로 생성해 드리겠습니다.